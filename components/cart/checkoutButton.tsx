import OffsetButton from "../utilityComponent/offsetButton";

export default function CheckoutButton(){
    return(
        <OffsetButton className="bg-green-600 hover:bg-green-800">
          Check Out
        </OffsetButton>
    )
}